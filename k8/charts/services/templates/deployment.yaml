apiVersion: apps/v1
kind: Deployment
metadata:
  name: binance-order-tracker
  labels:
{{ include "mychart.labels" . | indent 4 }}
    binance-tool/service: "binance-order-tracker"
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "mychart.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "mychart.name" . }}
        app.kubernetes.io/instance: {{ .Release.Name }}
        binance-tool/service: "binance-order-tracker"
      annotations:
        ad.datadoghq.com/tags: '{"service": "binance-order-tracker"}'
        ad.datadoghq.com/binance-order-tracker.logs: >-
          [{
            "source": "nodejs",
            "service": "binance-order-tracker"
          }]   
    spec:
    {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
    {{- end }}
      volumes:
      containers:
        - name: binance-order-tracker
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: ["./services/order-tracker/binance-order-tracker.ts", "--live"]
          # ports:
          # - name: http
          #   containerPort: 8081
          #   protocol: TCP
          # livenessProbe:
          #   httpGet:
          #     path: /health
          #     port: http
          # readinessProbe:
          #   httpGet:
          #     path: /health
          #     port: http
          env:
            - name: SENTRY_DSN
              value: "https://fdceaf9d5e984c6aad15f6ec0301f1f5@o369902.ingest.sentry.io/5286784"
{{- include "binance.vars" . | nindent 12 }}
{{- include "datadog.vars" . | nindent 12 }}
{{- include "telegram.vars" . | nindent 12 }}
# # ---
# # apiVersion: apps/v1
# # kind: Deployment
# # metadata:
# #   name: redis-monitor
# #   labels:
# # {{ include "mychart.labels" . | indent 4 }}
# #     binance-tool/service: "redis-monitor"
# # spec:
# #   replicas: {{ .Values.replicaCount }}
# #   selector:
# #     matchLabels:
# #       app.kubernetes.io/name: {{ include "mychart.name" . }}
# #       app.kubernetes.io/instance: {{ .Release.Name }}
# #   template:
# #     metadata:
# #       labels:
# #         app.kubernetes.io/name: {{ include "mychart.name" . }}
# #         app.kubernetes.io/instance: {{ .Release.Name }}
# #         binance-tool/service: "redis-monitor"
# #       annotations:
# #         ad.datadoghq.com/tags: '{"service": "redis-monitor"}'
# #         ad.datadoghq.com/redis-monitor.logs: >-
# #           [{
# #             "source": "nodejs",
# #             "service": "redis-monitor"
# #           }]  
# #     spec:
# #       volumes:
# #       containers:
# #         - name: redis-monitor
# #           image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
# #           imagePullPolicy: {{ .Values.image.pullPolicy }}
# #           command: ["./services/redis-monitor.ts"]
# #           env:
# #             - name: SENTRY_DSN
# #               value: "https://673cf6fd7c5e49339128d0f4bb3f37c7@o369902.ingest.sentry.io/5286786"
# # {{- include "datadog.vars" . | nindent 12 }}
# # {{- include "redis.vars" . | nindent 12 }}
# # {{- include "telegram.vars" . | nindent 12 }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: portfolio-tracker
  labels:
{{ include "mychart.labels" . | indent 4 }}
    binance-tool/service: "portfolio-tracker"
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "mychart.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "mychart.name" . }}
        app.kubernetes.io/instance: {{ .Release.Name }}
        binance-tool/service: "portfolio-tracker"
      annotations:
        ad.datadoghq.com/tags: '{"service": "portfolio-tracker"}'
        ad.datadoghq.com/portfolio-tracker.logs: >-
          [{
            "source": "nodejs",
            "service": "portfolio-tracker"
          }]   
    spec:
    {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
    {{- end }}
      volumes:
      containers:
        - name: portfolio-tracker
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: ["./services/portfolio_tracker/portfolio-tracker.ts", "--live"]
          ports:
            - name: http
              containerPort: 80
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /health
              port: http
          # readinessProbe:
          #   httpGet:
          #     path: /health
          #     port: http
          env:
            - name: SENTRY_DSN
              value: "https://fd044b9bc259422ea55a1910e3801aa8@o369902.ingest.sentry.io/5618730"
{{- include "binance.vars" . | nindent 12 }}
{{- include "datadog.vars" . | nindent 12 }}
{{- include "telegram.vars" . | nindent 12 }}
{{- include "amqp.vars" . | nindent 12 }}
{{- include "redis.vars" . | nindent 12 }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: binance-to-amqp
  labels:
{{ include "mychart.labels" . | indent 4 }}
    binance-tool/service: "binance-to-amqp"
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "mychart.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "mychart.name" . }}
        app.kubernetes.io/instance: {{ .Release.Name }}
        binance-tool/service: "binance-to-amqp"
      annotations:
        ad.datadoghq.com/tags: '{"service": "binance-to-amqp"}'
        ad.datadoghq.com/binance-to-amqp.logs: >-
          [{
            "source": "nodejs",
            "service": "binance-to-amqp"
          }]   
    spec:
    {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
    {{- end }}
      volumes:
      containers:
        - name: binance-to-amqp
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: ["./services/binance-to-amqp/binance-to-amqp.ts", "--live"]
          ports:
            - name: http
              containerPort: 80
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /health
              port: http
          # readinessProbe:
          #   httpGet:
          #     path: /health
          #     port: http
          env:
            - name: SENTRY_DSN
              value: "https://fd044b9bc259422ea55a1910e3801aa8@o369902.ingest.sentry.io/5618730"
{{- include "binance.vars" . | nindent 12 }}
{{- include "datadog.vars" . | nindent 12 }}
{{- include "telegram.vars" . | nindent 12 }}
{{- include "amqp.vars" . | nindent 12 }}
{{- include "redis.vars" . | nindent 12 }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: binance-position-tracker
  labels:
{{ include "mychart.labels" . | indent 4 }}
    binance-tool/service: "binance-position-tracker"
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "mychart.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "mychart.name" . }}
        app.kubernetes.io/instance: {{ .Release.Name }}
        binance-tool/service: "binance-position-tracker"
      annotations:
        ad.datadoghq.com/tags: '{"service": "binance-position-tracker"}'
        ad.datadoghq.com/binance-position-tracker.logs: >-
          [{
            "source": "nodejs",
            "service": "binance-position-tracker"
          }]   
    spec:
    {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
    {{- end }}
      volumes:
      containers:
        - name: binance-position-tracker
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: ["./services/position-tracker/binance-position-tracker.ts", "--live"]
          # ports:
          # - name: http
          #   containerPort: 8081
          #   protocol: TCP
          # livenessProbe:
          #   httpGet:
          #     path: /health
          #     port: http
          # readinessProbe:
          #   httpGet:
          #     path: /health
          #     port: http
          env:
            - name: SENTRY_DSN
              value: "https://f999d04298a34caa82c13839699a266f@o369902.ingest.sentry.io/5735251"
{{- include "binance.vars" . | nindent 12 }}
{{- include "datadog.vars" . | nindent 12 }}
{{- include "telegram.vars" . | nindent 12 }}
{{- include "amqp.vars" . | nindent 12 }}
{{- include "redis.vars" . | nindent 12 }}
# ---
# apiVersion: apps/v1
# kind: Deployment
# metadata:
#   name: auto-position-exits
#   labels:
# {{ include "mychart.labels" . | indent 4 }}
#     binance-tool/service: "auto-position-exits"
# spec:
#   replicas: {{ .Values.replicaCount }}
#   selector:
#     matchLabels:
#       app.kubernetes.io/name: {{ include "mychart.name" . }}
#       app.kubernetes.io/instance: {{ .Release.Name }}
#   template:
#     metadata:
#       labels:
#         app.kubernetes.io/name: {{ include "mychart.name" . }}
#         app.kubernetes.io/instance: {{ .Release.Name }}
#         binance-tool/service: "auto-position-exits"
#       annotations:
#         ad.datadoghq.com/tags: '{"service": "auto-position-exits"}'
#         ad.datadoghq.com/auto-position-exits.logs: >-
#           [{
#             "source": "nodejs",
#             "service": "auto-position-exits"
#           }]   
#     spec:
#     {{- with .Values.imagePullSecrets }}
#       imagePullSecrets:
#         {{- toYaml . | nindent 8 }}
#     {{- end }}
#       containers:
#         - name: auto-position-exits
#           image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
#           imagePullPolicy: {{ .Values.image.pullPolicy }}
#           command: ["./services/auto-position-exits/auto-position-exits.ts", "--live"]
#           # ports:
#           # - name: http
#           #   containerPort: 8081
#           #   protocol: TCP
#           # livenessProbe:
#           #   httpGet:
#           #     path: /health
#           #     port: http
#           # readinessProbe:
#           #   httpGet:
#           #     path: /health
#           #     port: http
#           env:
#             - name: SENTRY_DSN
#               value: "https://b8ae7f07a03044cba474efd640e744d8@o369902.ingest.sentry.io/5735252"
# {{- include "binance.vars" . | nindent 12 }}
# {{- include "datadog.vars" . | nindent 12 }}
# {{- include "telegram.vars" . | nindent 12 }}
# {{- include "amqp.vars" . | nindent 12 }}
# {{- include "redis.vars" . | nindent 12 }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: edge56
  labels:
{{ include "mychart.labels" . | indent 4 }}
    binance-tool/service: "edge56"
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "mychart.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "mychart.name" . }}
        app.kubernetes.io/instance: {{ .Release.Name }}
        binance-tool/service: "edge56"
      annotations:
        ad.datadoghq.com/tags: '{"service": "edge56"}'
        ad.datadoghq.com/edge56.logs: >-
          [{
            "source": "nodejs",
            "service": "edge56"
          }]   
    spec:
    {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
    {{- end }}
      volumes:
      containers:
        - name: edge56
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: ["./services/edge56/edge56-service.ts", "--live"]
          # ports:
          # - name: http
          #   containerPort: 8081
          #   protocol: TCP
          # livenessProbe:
          #   httpGet:
          #     path: /health
          #     port: http
          # readinessProbe:
          #   httpGet:
          #     path: /health
          #     port: http
          env:
            - name: SENTRY_DSN
              value: "https://2de8658dca5b4bf18c7787cfd2deb738@o369902.ingest.sentry.io/5763740"
{{- include "binance.vars" . | nindent 12 }}
{{- include "datadog.vars" . | nindent 12 }}
{{- include "telegram.vars" . | nindent 12 }}
{{- include "amqp.vars" . | nindent 12 }}
{{- include "redis.vars" . | nindent 12 }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: position-performance
  labels:
{{ include "mychart.labels" . | indent 4 }}
    binance-tool/service: "position-performance"
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "mychart.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "mychart.name" . }}
        app.kubernetes.io/instance: {{ .Release.Name }}
        binance-tool/service: "position-performance"
      annotations:
        ad.datadoghq.com/tags: '{"service": "position-performance"}'
        ad.datadoghq.com/position-performance.logs: >-
          [{
            "source": "nodejs",
            "service": "position-performance"
          }]   
    spec:
    {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
    {{- end }}
      containers:
        - name: position-performance
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: ["./services/position-performance/position-performance-service.ts", "--live"]
          # ports:
          # - name: http
          #   containerPort: 8081
          #   protocol: TCP
          # livenessProbe:
          #   httpGet:
          #     path: /health
          #     port: http
          # readinessProbe:
          #   httpGet:
          #     path: /health
          #     port: http
          env:
            - name: SENTRY_DSN
              value: "https://2de8658dca5b4bf18c7787cfd2deb738@o369902.ingest.sentry.io/5763740"
{{- include "binance.vars" . | nindent 12 }}
{{- include "datadog.vars" . | nindent 12 }}
{{- include "telegram.vars" . | nindent 12 }}
{{- include "amqp.vars" . | nindent 12 }}
{{- include "redis.vars" . | nindent 12 }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: event-logger
  labels:
{{ include "mychart.labels" . | indent 4 }}
    binance-tool/service: "event-logger"
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "mychart.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "mychart.name" . }}
        app.kubernetes.io/instance: {{ .Release.Name }}
        binance-tool/service: "event-logger"
      annotations:
        ad.datadoghq.com/tags: '{"service": "event-logger"}'
        ad.datadoghq.com/event-logger.logs: >-
          [{
            "source": "nodejs",
            "service": "event-logger"
          }]   
    spec:
    {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
    {{- end }}
      containers:
        - name: event-logger
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: ["./services/event-logger/event-logger-service.ts", "--live"]
          ports:
          - name: http
            containerPort: 80
            protocol: TCP
          livenessProbe:
            httpGet:
              path: /health
              port: http
          # readinessProbe:
          #   httpGet:
          #     path: /health
          #     port: http
          env:
            - name: SENTRY_DSN
              value: "https://2de8658dca5b4bf18c7787cfd2deb738@o369902.ingest.sentry.io/5763740"
            - name: AWS_ACCESS_KEY_ID
              valueFrom:
                secretKeyRef:
                  key: AWS_ACCESS_KEY_ID
                  name: aws-event-logger
            - name: AWS_SECRET_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  key: AWS_SECRET_ACCESS_KEY
                  name: aws-event-logger
{{- include "amqp.vars" . | nindent 12 }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: portfolio-to-influxdb
  labels:
{{ include "mychart.labels" . | indent 4 }}
    binance-tool/service: "portfolio-to-influxdb"
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "mychart.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "mychart.name" . }}
        app.kubernetes.io/instance: {{ .Release.Name }}
        binance-tool/service: "portfolio-to-influxdb"
      annotations:
        ad.datadoghq.com/tags: '{"service": "portfolio-to-influxdb"}'
        ad.datadoghq.com/portfolio-to-influxdb.logs: >-
          [{
            "source": "nodejs",
            "service": "portfolio-to-influxdb"
          }]   
    spec:
    {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
    {{- end }}
      containers:
        - name: portfolio-to-influxdb
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: ["./services/portfolio-to-influxdb/portfolio-to-influxdb-service.ts"]
          ports:
          - name: http
            containerPort: 80
            protocol: TCP
          livenessProbe:
            httpGet:
              path: /health
              port: http
          # readinessProbe:
          #   httpGet:
          #     path: /health
          #     port: http
          env:
            - name: SENTRY_DSN
              value: "https://2de8658dca5b4bf18c7787cfd2deb738@o369902.ingest.sentry.io/5763740" # TODO
{{- include "amqp.vars" . | nindent 12 }}
{{- include "influxdb.vars" . | nindent 12 }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: tg-bot-bert
  labels:
{{ include "mychart.labels" . | indent 4 }}
    binance-tool/service: "tg-bot-bert"
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "mychart.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "mychart.name" . }}
        app.kubernetes.io/instance: {{ .Release.Name }}
        binance-tool/service: "tg-bot-bert"
      annotations:
        ad.datadoghq.com/tags: '{"service": "tg-bot-bert"}'
        ad.datadoghq.com/tg-bot-bert.logs: >-
          [{
            "source": "nodejs",
            "service": "tg-bot-bert"
          }]   
    spec:
    {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
    {{- end }}
      containers:
        - name: tg-bot-bert
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: ["./services/tg-bot-bert-2000/bert.ts", "--live"]
          ports:
          - name: http
            containerPort: 3000
            protocol: TCP
          # livenessProbe:
          #   httpGet:
          #     path: /health
          #     port: http
          # readinessProbe:
          #   httpGet:
          #     path: /health
          #     port: http
          env:
            - name: SENTRY_DSN
              value: "https://2de8658dca5b4bf18c7787cfd2deb738@o369902.ingest.sentry.io/5763740" # TODO update
{{- include "telegram-bot-bert.vars" . | nindent 12 }}
